#include "holberton.h"
#include <stdio.h>
/**
 * _calloc - allocates memory for an array
 * @size: size of int argument
 * @nmemb: array
 *
 * Return: NULL
 **/

void *_calloc(unsigned int nmemb, unsigned int size);
{
	void *sampptr;

	int i;

	if (nmemb == 0 || size == 0)
	{
		return (NULL);
	}

	if (malloc
}
