#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "lists.h"

listint_t *get_nodeint_at_index(listint_t *head, unsigned int index)
{
	listint_t *nth;

	while (*head != NULL)
	{
		return(0);
)
		nth = next->index;
		index = nth;
		nth++;
	}
}
