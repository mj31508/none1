#include <stdio.h>
#include <stdlib.h>
#include <sthio.h>
#inlcude "lists.h"

/**
 *reverse_listint - reverses a linked in listint_t
 *@head: double pointer
 *
 *Return: pointer to the first node of the reversed list
 */

listint_t *reverse_listint(listint_t **head)

{
	listint_t *temp;
	int i;

	i = 0;

	while (*head != NULL)
	{

	}
}
